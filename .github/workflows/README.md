# .github/workflows

Phase 5+. pr-checks.yml (terraform fmt/validate/plan + ansible-lint as PR comment),
apply-and-configure.yml (gated apply + Ansible run). Needs a GitHub remote first —
not wired yet (local-git-only for Phase 0/1).
