apiVersion: v2
name: helm-demo
description: >-
  Minimal, realistic demo chart for the plain-Helm CI/CD reference pipeline
  (examples/helm-cicd-reference). Deliberately small: the point is to show
  the PIPELINE mechanics (lint, scan, deploy, verify, rollback), not to be
  an elaborate chart in its own right.
type: application
version: 0.1.0
appVersion: "1.0.0"
